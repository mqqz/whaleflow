# WhaleFlow Backend

To run the app (while in this `backend/` directory),
get the needed dependancies from `requirements.txt` and then run:
```
uvicorn app:app --reload
```

To backfill the database run the `backfill.py` script.
